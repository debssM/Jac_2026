## <Project Name>
Turbo tax for fraud claimants. Built using jaclang.

## Run the pipeline using the following commands
Backend:
`pip install requirements.txt`
`python server.py`

Frontend:
`npm install`
`npm run dev`
